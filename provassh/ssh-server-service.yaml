# ssh-server-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: ssh-server-service # Nome DNS che userà il client
  namespace: ssh-demo
spec:
  selector:
    app: ssh-server
  ports:
    - protocol: TCP
      port: 22
      targetPort: 22
  type: ClusterIP